version: "3.7"         

services:        
  db:             
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: database
    environment:
      MSSQL_SA_PASSWORD: Un!q@to2023  
      # MSSQL_DATABASE: db_vero_digital     
      ACCEPT_EULA: Y
    networks:
      - salihnet

  myapp:
    image: salihijk/quickdbtest:latest
    container_name: myapp
    restart: always
    depends_on:
      - db
    ports:
      - "80:80"
    networks:
      - salihnet

networks:    
  salihnet:      
    driver: bridge